<%= simple_form_for [@proposal, @terms_condition], defaults: { wrapper_html: { class: 'form-group' }, input_html: {class: 'form-control'} }, html: { multipart: true, novalidate: true } do |tc| %>
  <form role="form">
      <div class="field">
        <%= tc.input :name %>
       </div>
       <div class="field">
        <%= tc.input :content %>
       </div>
    </div>
    <%= tc.submit class: "btn btn-primary" %>
  </form>
  <% end %>
</div>
